# docker-compose.yml
version: "3.8"

services:
  mongodb:
    image: mongo:6
    container_name: wt2-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data:
